# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

platform :ios, '16.0'
target 'Walking-faster' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Walking-faster
  pod 'GoogleMaps', '8.3.0'
  pod 'GooglePlaces'

  target 'Walking-fasterTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Walking-fasterUITests' do
    # Pods for testing
  end

end
